<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E9E2EE"> <!-- Agent Theme Background -->

    <!-- 1. FIXED HEADER AREA -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#E9E2EE"
        app:elevation="0dp"
        app:liftOnScroll="false"
        android:theme="@style/ThemeOverlay.MaterialComponents.Toolbar.Primary">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#E9E2EE"
            android:paddingBottom="24dp">

            <!-- Header Banner -->
            <FrameLayout
                android:id="@+id/headerBanner"
                android:layout_width="match_parent"
                android:layout_height="140dp"
                android:background="#B792D9">
                <TextView
                    android:id="@+id/tvHeaderTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginTop="24dp"
                    android:text="Agent Profile"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    android:textSize="32sp" />
            </FrameLayout>

            <!-- Profile Content (Overlapping the banner) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:layout_marginTop="80dp">

                <com.google.android.material.imageview.ShapeableImageView
                    android:id="@+id/ivProfileImage"
                    android:layout_width="110dp"
                    android:layout_height="110dp"
                    android:padding="2dp"
                    android:scaleType="centerCrop"
                    android:src="@drawable/ic_profile_tenant"
                    app:strokeColor="#FFFFFF"
                    app:strokeWidth="4dp"
                    app:shapeAppearanceOverlay="@style/CircleImage" />

                <!-- Company uses tvUserName spot -->
                <TextView
                    android:id="@+id/tvUserName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="Letting Agency Name"
                    android:textColor="#1A1A1A"
                    android:textSize="24sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tvUserEmail"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="agent@email.com"
                    android:textColor="#555555"
                    android:textSize="14sp" />
            </LinearLayout>
        </RelativeLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- 2. SCROLLABLE CONTENT AREA -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#E9E2EE"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="20dp"
            android:paddingTop="10dp"
            android:paddingBottom="100dp">

            <!-- Account Settings Card (Removed Edit Name) -->
            <TextView style="@style/SettingsHeaderStyle" android:text="Account Settings" />
            <androidx.cardview.widget.CardView style="@style/SettingsCardStyle">
                <LinearLayout android:orientation="vertical" android:layout_width="match_parent" android:layout_height="wrap_content">
                    <include android:id="@+id/rowEditPassword" layout="@layout/layout_settings_item" />
                    <View style="@style/DividerStyle" />
                    <include android:id="@+id/rowEditImage" layout="@layout/layout_settings_item" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>

            <!-- Support & Legal Card -->
            <TextView style="@style/SettingsHeaderStyle" android:text="Support &amp; Legal" android:layout_marginTop="24dp" />
            <androidx.cardview.widget.CardView style="@style/SettingsCardStyle">
                <LinearLayout android:orientation="vertical" android:layout_width="match_parent" android:layout_height="wrap_content">
                    <include android:id="@+id/rowAbout" layout="@layout/layout_settings_item" />
                    <View style="@style/DividerStyle" />
                    <include android:id="@+id/rowFaq" layout="@layout/layout_settings_item" />
                    <View style="@style/DividerStyle" />
                    <include android:id="@+id/rowPrivacy" layout="@layout/layout_settings_item" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>

            <!-- Logout Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnLogout"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginTop="32dp"
                android:layout_marginBottom="20dp"
                android:text="Logout"
                android:textColor="#FFFFFF"
                app:backgroundTint="#B792D9"
                app:cornerRadius="15dp"
                app:elevation="0dp"
                android:textAllCaps="false"
                android:textStyle="bold" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- Bottom Navigation View -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="#FFFFFF"
        android:elevation="16dp"
        android:layout_gravity="bottom"
        app:menu="@menu/agent_bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        style="@style/Widget.Design.BottomNavigationView"
        app:itemBackground="@drawable/bg_nav_pill_custom"
        app:itemIconSize="50dp"
        app:itemIconTint="@color/selector_nav_icon"
        app:itemTextColor="#000000" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
